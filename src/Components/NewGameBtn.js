import classes from './NewGameBtn.module.css';

const NewGameBtn = () => {

    const clickHandler = () => {

    }

    return (
        <button className={classes.NewGameBtn} onClick={clickHandler}/>
    )
}

export default NewGameBtn